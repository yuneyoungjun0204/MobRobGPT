%============================================================ 1. 서론
\section{서론}
\label{sec:intro}

\subsection{배경}

다수의 저비용 무인수상정(Unmanned Surface Vehicle, USV)이 고가치 함정 한 척을 향해 여러
방향에서 동시에 돌진하는 자폭 공격은 대표적인 비대칭 위협이다. 유한한 방어 자산을 다수
표적에 배분하는 문제는 무기-표적 할당(weapon-target assignment)으로 오래 연구되어
왔으나~\citep{ahuja2007wta}, 표적 수가 요격 자산을 넘어서면 최적 할당으로도 처리되지 않는
표적이 남는다.

무인 위협에 대한 기존 대응은 두 갈래로 구분된다~\citep{wang2021cuas}. 표적의 통신·항법
·센서를 교란하는 연성 대응(soft-kill)은 위협 자체를 제거하지 못하며, 전파를 방사하지 않는
유선 조종 표적에는 작용하지 않는다~\citep{sutea2026fiber}. 표적을 직접 타격하는 경성
대응(hard-kill)은 위협을 확실히 제거하지만, 수천 달러 규모의 표적에 발당 수십만에서
수백만 달러의 요격 유도탄을 소모하는 비용 구조를 지탱하기 어렵고~\citep{rumbaugh2024cost},
한 번에 한 표적만 처리하므로 표적 수가 교전 용량을 넘어서면 포화가 발생한다.

본 연구는 대안으로 저비용 그물을 이용한 물리적 포획을 채택한다. 이는 무인기 대응 분야에서
독립된 대응 범주로 다루어져 왔으며~\citep{wang2021cuas}, 표적의 통신 방식과 무관하게
작용하고 표적을 정지시킨다는 점에서 앞의 두 한계를 비켜간다. 방어정은 적선보다 느리므로
추격으로 따라잡을 수 없고, 대신 적선이 지나갈 접근 회랑을 앞질러 그물벽으로 차단한다
(Fig.~\ref{fig:overview}).

\subsection{문제의식}

이 방어 문제는 성격이 다른 두 판단을 동시에 요구한다. 하나는 \emph{어느 방어정이 어느
적 무리를 맡을 것인가}라는 이산적·전술적 배정이고, 다른 하나는 \emph{맡은 무리를 막기 위해
해면의 어느 지점에 그물벽을 놓을 것인가}라는 연속적·기하적 기동이다. 전자는 전장 전체의
맥락과 우선순위 판단을 요구하고, 후자는 국소적이지만 매 결정마다 새로 계산되어야 한다.
두 판단은 같은 주기로 갱신되지만 \emph{산출에 걸리는 시간이 크게 다르다}. 본 연구는 느린 쪽인 배정을 언어모델 지휘관에게, 빠른 쪽인 기동을 학습된 정책에 맡긴다.(Fig.~\ref{fig:overview})

\begin{figure*}[tp]
\centering
\includegraphics[width=0.9\textwidth]{fig1.png}
\caption{제안 체계의 개요. 위: 전략 계층 --- 언어모델 지휘관이 전장 상태·임무 목표·교전 규칙·자산 상태를 입력받아 \emph{어느 방어정이 어느 무리를 맡는가}(배정)와 대기 여부를 구조화된 출력으로 낸다. 아래: 기동 계층 --- 배정을 조건으로 학습된 정책이 각 방어정의 그물벽 위치(경유점 두 개)를 정한다. 방어정은 적선보다 느리므로 추격 대신 접근 회랑을 앞질러 그물벽으로 차단한다. 그림의 함정 형상과 패널은 개념 전달용 삽화이며, 실제 입출력 형식은 \S\ref{sec:method}에 정의한다.}
\label{fig:overview}
\end{figure*}

\subsection{배치를 전제로 한 설계}

본 연구는 시뮬레이션 안에서만 성립하는 체계를 목표로 하지 않았다. 방법을 고를 때마다
\emph{온보드에서 돌 수 있는가}를 함께 물었고, 그 선택들이 본문 곳곳에 흩어져 있으므로
여기에 모아 둔다.

\begin{itemize}[leftmargin=*, itemsep=2pt, topsep=3pt]
\item \textbf{실해역 지형에서 학습·평가한다.} 교전 해역은 가상의 빈 바다가 아니라
      OpenStreetMap 의 실제 해안선(통영 매물도 인근)을 래스터화한 것이며, 육지는 충돌
      판정과 관측 채널 양쪽에 들어간다(\S\ref{sec:obs}).
\item \textbf{전장을 이미지로 보고, 행동을 픽셀로 낸다.} 관측은 선박별 벡터의 나열이
      아니라 모선을 중앙에 둔 $50\times50$ 격자 위의 15채널 영상이고, 정책은 그 위에서
      픽셀을 지목하는 U-Net 구조이다. 행동은 연속 좌표의 회귀가 아니라 격자의 유효 픽셀
      가운데 하나를 고르는 \emph{이산적인 선택}이므로, 정책은 해면 전체에 대한 점수맵을
      낸다. 연속 좌표 회귀에서는 불가능 영역을 피하도록 학습하거나 사후에 투영해야 하지만,
      이산 선택에서는 그런 자리가 애초에 뽑히지 않게 막을 수 있다. 선박 수가 바뀌어도
      입력·출력의 형태가 변하지 않으며, 배치 통계에 의존하는 정규화를 쓰지 않아 배치 크기
      1의 온보드 추론이 학습과 같은 연산을 수행한다(\S\ref{sec:obs}, \S\ref{sec:deploy}).
\item \textbf{언어모델은 느린 판단만 맡고, 빠른 판단을 막지 않는다.} 배정은 언어모델
      지휘관이, 기동은 학습된 정책이 맡는다. 둘은 비동기로 분리되어 지휘관의 응답을
      기다리는 동안에도 기동은 멈추지 않는다 --- 응답 속도가 크게 다른 두 판단 계층을
      같은 결정 주기 안에서 결합한 것이다.
\item \textbf{출력이 곧 항법 명령이다.} 정책의 출력은 제어 신호가 아니라 경유점 두 개이며,
      이 형식은 특정 선체 동역학에 묶이지 않는다. 저수준 추종은 학습과 분리되어 있어
      실제 선체의 동역학으로 교체할 수 있고, 그만큼 시뮬레이션에서 실기로 옮기기 쉽다
      (\S\ref{sec:deploy}).
\end{itemize}

\subsection{기여}

본 논문의 기여는 \emph{무엇이 새로 가능해졌는가}로 정리된다.

\begin{enumerate}[leftmargin=*, itemsep=3pt, topsep=4pt]
\item \textbf{적보다 느린 소수의 방어정으로도 군집을 물리적으로 막을 수 있다.} 핵심은
      그물벽을 놓을 자리를 \emph{연속 좌표로 회귀하지 않고, 해면 격자의 픽셀 하나를
      이산적으로 고른다}는 행동 표현이다. 정책은 15채널 격자 관측을 FiLM 조건화 U-Net 에
      넣어 지도 전체에 대한 점수맵을 내고, 그 위에서 유효 픽셀 중 하나를 지목한다.
      불가능한 자리(육지, 사거리 밖)로 출력이 새거나 다봉 분포를 평균 내어 어느 쪽도
      아닌 자리를 내는 문제는, 이산 행동공간에 마스크를 씌워 발생 자체를 막았다. 그 결과
      같은 정도로 막으면서 포획당 그물 소모를 $44.5\,\%$, 총 선회량을 $46.6\,\%$ 줄였다.
      \textbf{이 가운데 언어모델 없이 기동 계층 단독으로 얻는 몫}은 포획당 그물 $22.9\,\%$,
      그물 걸림 $24.3\,\%$ 감소이다.
\item \textbf{방어정 수와 적선 수가 바뀌어도 같은 모델이 돈다.} 관측을 선박별 벡터의
      나열이 아니라 \emph{이미지 형식}의 15채널 격자로 바꾼 것이 핵심이다. 전장의 선박
      수가 몇 척이든 관측은 같은 크기의 격자 위에 그려지므로 입력의 형태가 유지되고, 입력
      순열 문제 자체가 생기지 않는다. 가변 개수 입력을 다루는 기존 방식들인 최대 개수로
      패딩하고 마스크를 붙이거나 개체 목록 위에 어텐션을 얹는 방식과 달리, 해당 방식으로는
      개체 수에 따라 달라지는 것이 아무것도 없다. 계산량이 선박 수에 비례해 늘지 않고,
      마스크나 최대 개수 같은 설계 상수를 둘 필요가 없다. 또한 개체로 표현되지 않는
      정보(해안선·섬 같은 육지, 모선 주변의 위협 분포)도 같은 격자에 채널 하나로 얹힌다.
      무엇보다 출력이 같은 격자 위의 픽셀이므로 관측과 행동이 하나의 좌표계를 공유하게
      된다. 여기에 방어정 간 파라미터 공유를 더해 모델 크기 역시 방어정 수에 불변이다(총
      244{,}389개). 그 결과 동일한 하나의 모델이 어떤 편성·어떤 상황에서도 강건하게
      대응할 수 있다.
\item \textbf{가치망 없이 팀 보상 하나로 다중 방어정의 협동 기동을 학습하였다.} 본
      연구는 그룹 상대 정책 최적화를 다중 방어정으로 확장하되, 결합 행동 후보 하나를 기하
      휴리스틱으로 고정하여 기준선으로 삼는다. 각 후보를 시뮬레이터에서 실제로 진행시켜
      얻은 팀 보상을 휴리스틱과 비교하므로, 휴리스틱과 다르게 움직인 방어정의 선택만이
      이득의 차이를 만들고, 학습된 비평가로 가치를 추정하지 않으므로 가치 추정 편향과
      비평가 학습의 불안정성이 설계에서 사라진다. 이 기준선은 정책을 휴리스틱 근방에
      붙잡는 신뢰 영역 구실도 하여, 무작위 초기화에서 흔한 초기 붕괴 없이 휴리스틱
      천장을 넘어서게 한다. 한 척의 행동만 바꾸어 재평가하는 반사실적 변형도 구현하여
      비교하였다.
\item \textbf{규칙으로 적기 어려운 전술 판단을 지휘 계층에 맡기고, 그 효과와 평가 기준을
      수치로 제시한다.} 어느 방어정이 어느 무리를 맡을지는 각 무리의 접근 방위와
      요격까지의 거리·선회량, 이미 놓인 그물의 차단 효과, 방어정별 잔여 자산과 교전
      규칙을 함께 저울질해야 하는 판단이며, 이를 고정된 규칙과 조합 최적화로 전부
      열거하기는 어렵다. 본 연구는 이 유연한 판단을 지휘관에게 온전히
      맡기고 720 에피소드를 시드 대응표본으로 수집해 두 계층의 기여를 분리 측정하였다.
\end{enumerate}

\noindent
논문의 구성은 다음과 같다. \S\ref{sec:related}에서 관련 연구를 정리하고,
\S\ref{sec:problem}에서 문제와 평가 지표를 정의한다. \S\ref{sec:method}에서 제안 체계를
기술하고, \S\ref{sec:experiment}에서 실험 설계와 통계 절차를 밝힌다.
\S\ref{sec:results}에서 결과를, \S\ref{sec:discussion}에서 해석을 제시하고,
\S\ref{sec:limitations}에서 한계를 논의한다.
